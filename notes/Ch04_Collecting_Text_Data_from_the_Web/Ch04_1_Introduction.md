## Ch04.1 Introduction



> **本章概要**
>
> - 从网页提取数据并进行处理的方法
> - 描述不同类型的半结构化数据（`JSON`、`XML` 等）
> - 利用 `API` 接口获取实时数据
> - 从各种文件格式中获取数据

本章学习通过网页爬虫收集数据并进行处理的方法，同时学习如何处理 `JSON`、`XML` 等各类半结构化数据；还将探讨利用 `API` 接口提取数据的不同方法，最后还将探索从各类文件中提取数据的多种途径。
